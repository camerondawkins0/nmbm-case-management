# Cloud SQL for Postgres (or local Postgres for dev)
DATABASE_URL=postgres://nmbm:nmbm@localhost:55432/nmbm_cms

# Google OIDC (Workspace SSO) — from a GCP OAuth client, not a personal one
GOOGLE_OIDC_CLIENT_ID=
GOOGLE_OIDC_CLIENT_SECRET=
GOOGLE_OIDC_REDIRECT_URI=http://localhost:8080/auth/google/callback
# NMBM's Workspace domain. Sign-in refuses to start without it rather
# than accepting any Google account — still unanswered by NMBM.
GOOGLE_WORKSPACE_DOMAIN=

# Session cookie signing secret
SESSION_SECRET=

# Cloud Storage bucket for documents/consent uploads (GCS bucket name, not a URL)
DOCUMENTS_BUCKET=

# Local development only. Enables /auth/dev-login?email=... which skips
# Google sign-in entirely. Ignored when NODE_ENV=production, and the
# route isn't registered at all unless this is exactly "true".
ALLOW_DEV_LOGIN=false
