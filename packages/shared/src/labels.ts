// M1 was left open in discovery: "Clients and we can call them cases —
// are there any thoughts to this?" The table underneath is always called
// `participants` (see @nmbm/db); this constant is the one thing to
// change once NMBM decides, so it doesn't require a migration.
export const PARTICIPANT_LABEL = "Client";
export const PARTICIPANT_LABEL_PLURAL = "Clients";
